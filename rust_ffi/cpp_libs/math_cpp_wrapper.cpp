#include "math_cpp_wrapper.h"
#include "math_cpp.h"

int add(int a, int b) {
  Math math;
  return math.add(a, b);
}

int sub(int a, int b) {
  Math math;
  return math.sub(a, b);
}

int mul(int a, int b) {
  Math math;
  return math.mul(a, b);
}

int div(int a, int b) {
  Math math;
  return math.div(a, b);
}
